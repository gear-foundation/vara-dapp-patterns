# JWT secret — minimum 32 characters in production
JWT_SECRET=change-me-to-a-long-random-string

# Vara network WebSocket endpoint
VARA_WS=wss://testnet.vara.network

# VFT (Vara Fungible Token) program ID used for balance gating
VFT_PROGRAM_ID=0x...

# Token decimals (0 for integer tokens, 18 for 18-decimal tokens)
VFT_DECIMALS=0

# Minimum token balance required to gain access (in human units)
VFT_THRESHOLD=3000

# Optional: expected domain in the signed message (leave empty to skip check)
EXPECTED_DOMAIN=myapp.com

# Optional: expected chain ID in the signed message (leave empty to skip check)
EXPECTED_CHAIN_ID=vara

# CORS origins (comma-separated, or * to allow all)
ALLOWED_ORIGINS=http://localhost:5173,https://myapp.com

# Nonce TTL in seconds (default: 600 = 10 minutes)
NONCE_TTL_SEC=600

# JWT validity in minutes (default: 20)
JWT_TTL_MIN=20

# Minimum seconds remaining before JWT is refreshed (default: 300 = 5 min)
REFRESH_MIN_REMAIN_SEC=300

# Whether to re-check VFT balance on token refresh (default: true)
RECHECK_ON_REFRESH=true

# Clock skew tolerance in milliseconds (default: 120000 = 2 minutes)
CLOCK_SKEW_MS=120000

# HTTP port (default: 3000)
PORT=3000
