# Vara network WebSocket endpoint
NODE_URL=wss://testnet.vara.network

# Hex-encoded seed of the account that pays for voucher issuance.
# This account must hold enough VARA to cover all vouchers it issues.
# Generate with: subkey generate --scheme sr25519
VOUCHER_ACCOUNT_SEED_HEX=0x...

# On-chain program ID (HexString) to scope all issued vouchers to.
PROGRAM_ID=0x...

# HTTP port (default: 3000)
PORT=3000
